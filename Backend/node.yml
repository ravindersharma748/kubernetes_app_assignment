apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodejs
  template:
    metadata:
      labels:
        app: nodejs
    spec:
      containers:
        - env:                   
            - name: MYSQL_DB     
              value: node_db     
            - name: MYSQL_HOST   
              value: mysql-app   
            - name: MYSQL_PASS   
              value: password    
            - name: MYSQL_USER   
              value: admin       
          name: nodejs-app
          image: localhost:5000/nodejs-app:1.0.0
          ports:
            - containerPort: 8005
---
apiVersion: v1
kind: Service
metadata:
  name: node
spec:
  type: NodePort 
  selector:
    app: nodejs
  ports:
    - port: 8005 # expose the service on internal port
      targetPort: 8005 # our NodeJS app listens on port 8005
      nodePort: 30000
